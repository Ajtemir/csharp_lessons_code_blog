using System;

namespace lesson_4
{
    class Program
    {
        static void Main(string[] args)
        {
            Console.WriteLine("Hello World!");
            // musn't start digit and unique
            // ? MaxSize : x
            // UpperCamelCase class method
            // lowerCamelCase local variable argument
            //
            // snake_case  
            // FAT_SNAKE_CASE constant
            // kebab-case 
            // sHungarianCase
            // ? UserName : imyapolzovatelya
            // 
            // string s = "3";
            // int i = s; // error
            // int j = 2049;
            // int q = 42;
            // byte b = (byte)q; // явное привидение переменных
            // byte bb = (byte)j; // error
            // ? byte => int неявное привидение: int => byte
            // int iii = Convert.ToInt32(); // преоброзование
            // int i = int.Parse("42");
            // int i = int.TryParse("43");
            // Неявное пребоброзвание
            // int i = 1;
            // string s = "str " + i;
            // string s = "str " + i.ToString();
            // bool b = true;
            // string s = b.ToString();
            // string s = "true";
            // bool b = Convert.ToBoolean(s);
        }
    }
}
